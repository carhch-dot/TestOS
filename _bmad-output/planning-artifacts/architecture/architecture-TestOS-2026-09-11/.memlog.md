---
scope: TestOS — Núcleo v1 + Extendido v1.1 (ver PRD prd-TestOS-2026-09-11)
purpose: Fijar invariantes que evitan que las 8 features del PRD se construyan de forma incompatible entre sí, para bmad-create-epics-and-stories y bmad-build
altitude: initiative
updated: 2026-09-11T16:06
---

- (event by user) Input: prd.md + addendum.md de prd-TestOS-2026-09-11 (status: final). Modo elegido: Coaching path. Entregable: solo la espina, sin material visual adicional
- (decision by user) Paradigma: monolito modular (no microservicios) — módulos con fronteras reales (Auth, Inventario, Relaciones, Topología, Auditoría, luego Solicitudes/Reportes/Import). Resuelve PRD Open Question #1. Rechazado: microservicios completos (sin necesidad real de escalar/operar servicios independientes para un solo builder)
- (version by coach) Stack verificado en la web (2026-09-11): NestJS 11, Prisma 8 (TS-nativo), PostgreSQL, React 19 + Vite, XyFlow (antes React Flow) para visualización de topología
- (decision by coach) Motor de BD: PostgreSQL con columnas JSONB para propiedades dinámicas por tipo de ítem. Resuelve PRD Open Question #2. Rechazado: base de grafos dedicada (innecesaria dado el límite de 5 niveles/5000 nodos ya fijado en el PRD)
- (constraint by user) Despliegue: Hostinger VPS con Dokploy (PaaS self-hosted basado en Docker/docker-compose). Dos aplicaciones Dokploy separadas (api=NestJS, web=React build) + recurso de base de datos Postgres provisto por Dokploy. Todo secreto/config vía variables de entorno inyectadas por Dokploy — nunca hardcodeado ni committeado
- (decision by user) Autenticación: JWT con access token corto + refresh token rotado y lista de revocación persistida en Postgres (necesaria para cumplir la NFR de revocación explícita de sesión — JWT puro sin persistencia no la cumple). Resuelve PRD Open Question #3. Elegido por el usuario sobre la alternativa recomendada (sesiones server-side) — más superficie de implementación (rotación, detección de reuso) a cambio del patrón que quería usar
- (decision by user) Multi-tenant: NO. Instancia única, tal como ya estaba implícito en el PRD (roles por usuario, sin entidad Organización). El usuario consideró multi-tenant desde el inicio pero, al señalar que el PRD no modela organizaciones, decidió mantener instancia única en vez de reabrir el PRD. Resuelve PRD Open Question #4. Sin cambios necesarios al PRD
- (event by coach) Espina distilada: 7 ADs (monolito modular, frontera API, atomicidad de auditoría, RBAC guard, JWT+refresh con revocación, instancia única, config vía entorno), stack verificado en la web, árbol de fuente, ERD, diagrama de despliegue Dokploy/Hostinger, mapa capability→arquitectura, y Deferred con 8 ítems
- (event by coach) Reconciliación contra PRD encontró 3 huecos reales (JSONB para propiedades dinámicas nunca se fijó como AD pese a estar decidido en memlog; normalización de datos del PRD §10 sin convención; resiliencia SMTP del PRD §10 sin contrato de comportamiento) + 2 menores (excepción de auditoría en reimport, arranque ordenado). Agregados AD-8 (JSONB), AD-9 (MailModule con timeout/no-bloqueante), extendido AD-3 con la excepción de FR-29, y 2 convenciones nuevas (normalización, health check de arranque)
- (event by coach) Reviewer gate (verificación de versiones + ataque adversarial): FAIL inicial — Prisma 8 estaba en RC (no estable, corregido a 7.10), NestJS 12 salió 2026-08-27 (se mantiene 11 deliberadamente, documentado), Vite y PostgreSQL desactualizados (corregidos a 8.x y 18.x), AD-1 no cerraba el hueco de PrismaService compartido (agregado: un dueño por entidad), AD-3 no forzaba propagar el cliente de transacción entre módulos (agregado) ni una forma canónica de payload de auditoría (agregada). Medios: un controller por entidad, concurrencia en ItemConfiguracion diferida a cuando exista ChangeRequestsModule
- (event by coach) Lint determinista limpio tras reordenar AD-8/AD-9 en orden ascendente de aparición. Espina finalizada
- (event by coach) spine finalized
